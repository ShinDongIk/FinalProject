<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="myPageMapper">

	<!-- 리뷰
	<select id="selectReviewListCount" resultType="int">
	select count (*)
	from review
	where REVIEW_STATUS = 'Y'
	</select>
	
	
	<resultMap type="review" id="reviewResultSet">
		<result column="POST_NO" property="postNo"/>
		<result column="POST_RIVIEW" property="postReview"/>
		<result column="LOAD_DATE" property="loadDate"/>
	</resultMap>
	
	 
	<select id="selctReviewList" resultMap="reviewResultSet">
		SELECT POST_NO,POST_RIVIEW,LOAD_DATE
		FROM REVIEW
		WHERE REVIEW_STATUS='Y'
	</select>

 	-->

</mapper>